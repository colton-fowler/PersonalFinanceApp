# Navigation (`mobile/app`)

React Navigation stacks and screen wiring will live here in a later pass.

Auth was removed: this is a single-user local app with optional PIN/biometric lock planned under `mobile/security/`.
