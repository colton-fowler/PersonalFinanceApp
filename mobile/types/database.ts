/**
 * @deprecated Import domain types from `mobile/db` (models) instead.
 */
export type {
  TransactionCategory,
  Account,
  Transaction,
  Subscription,
  Setting,
  SettingKey,
} from "../db";

export { TRANSACTION_CATEGORIES, SETTING_KEYS } from "../db";
