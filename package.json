{
  "name": "rmoney",
  "version": "0.2.0",
  "private": true,
  "description": "Local-only personal finance app with private Plaid proxy. Install deps in mobile/ and server/ only.",
  "scripts": {
    "mobile": "npm run start --prefix mobile",
    "mobile:typecheck": "npm run typecheck --prefix mobile",
    "server": "npm run dev --prefix server",
    "server:typecheck": "npm run typecheck --prefix server"
  }
}
